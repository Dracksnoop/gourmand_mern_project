import HeroSection from "./HeroSection";
import RestaurantList from "./RestaurantList";

const Home = () => {
  return (
    <div>
      <HeroSection />
      <RestaurantList />
    </div>
  );
};

export default Home;
